{
  "name": "termino-hotel-api",
  "version": "1.0.0",
  "dependencies": {
    "@redocly/cli": "^1.2.0"
  },
  "private": true,
  "scripts": {
    "start": "redocly preview-docs --port 18088",
    "build": "redocly bundle -o dist/dist.yaml",
    "test": "redocly lint",
		"build-page": "redocly build-docs -o dist/index.html && cp docs/*.{png,svg} dist/"
  }
}
